# Phase 1 — 2 page mới (Lĩnh vực hoạt động, Phát triển bền vững) + gán nav — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dựng 2 trang phụ theo Figma (Lĩnh vực hoạt động, Phát triển bền vững) bằng đúng pattern Tailwind hiện có, rồi trỏ 2 mục nav tương ứng vào 2 trang đó.

**Architecture:** Mỗi trang là 1 page template thin (`get_header` → các `get_template_part` → `get_footer`), giống `page-ve-ecs.php`. Markup thật nằm trong `template-parts/`. Nội dung tĩnh trong `inc/data.php`. Tab của trang 1 tái dùng JS `initEcosystemTabs()` sẵn có; carousel trang 2 cần 1 hàm JS mới `initPtbvCarousel()`. Banner tiêu đề dùng chung 1 partial `page-hero.php`.

**Tech Stack:** WordPress classic PHP theme, Tailwind v4 (build qua `@tailwindcss/cli`), vanilla JS (IIFE trong `assets/js/main.js`), AOS + Headroom (vendored). PHP 8.3 (Laragon). WP-CLI.

## Global Constraints

- **Tailwind build bắt buộc:** sau MỌI thay đổi class Tailwind trong `.php` → chạy `npm run build:css` từ trong `ecsges/`, nếu không class sẽ KHÔNG được emit (Tailwind v4 chỉ scan `.php` và emit class xuất hiện literal). Giữ `npm run watch:css` khi làm việc.
- **Lint PHP** bằng php Laragon (không có `php` global): `"D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" -l <file>`.
- **WP-CLI:** `& "D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" "D:\wp-cli\wp-cli.phar" <cmd>` (chạy tại site root `d:\laragon\www\ECS`).
- **Escaping WP:** `esc_html`, `esc_attr`, `esc_url` cho mọi output; markup động theo chuẩn hiện có.
- **Màu brand:** cam `#f05a28` = class `bg-brand` / `text-brand` (đã định nghĩa trong `src/tailwind.css`). Token khác dùng lại: `max-w-page`, `bg-surface-2`, `text-ink`, `text-body`, `text-muted-2`, `bg-placeholder`, `font-display`.
- **Nội dung tĩnh** trong `inc/data.php` (KHÔNG ACF cho 2 page này) — theo precedent trang ve-ecs.
- **Không có test framework:** "verify" = `php -l` sạch + `npm run build:css` không lỗi + kiểm tra HTTP 200 và markup trên `http://ecs.test/...` bằng curl/trình duyệt.
- **Tên file page = slug** để WP tự áp template: `page-linh-vuc-hoat-dong.php`, `page-phat-trien-ben-vung.php` (giống `page-ve-ecs.php` ↔ slug `ve-ecs`).

---

## File Structure

**Tạo mới:**
- `template-parts/page-hero.php` — banner tiêu đề dùng chung (nhận `title`/`id`/`bg` qua `$args`).
- `template-parts/linh-vuc-tabs.php` — thanh 5 tab + panel (ảnh trái / text phải).
- `template-parts/ptbv-team.php` — carousel nhân sự.
- `template-parts/ptbv-values.php` — icon tròn + 3 giá trị TẬN TÂM / ĐỒNG HÀNH / ĐỔI MỚI.
- `page-linh-vuc-hoat-dong.php` — page template trang 1.
- `page-phat-trien-ben-vung.php` — page template trang 2.

**Sửa:**
- `inc/data.php` — thêm `ecsges_linh_vuc_tabs()`, `ecsges_ptbv_team()`, `ecsges_ptbv_values()`; đổi 2 `href` nav.
- `assets/js/main.js` — thêm `initPtbvCarousel()` + gọi trong `DOMContentLoaded`.

**Tái dùng nguyên (không sửa):** `template-parts/section-news.php`, `initEcosystemTabs()` trong `main.js`, `footer.php`, `header.php`.

---

## Task 1: Dữ liệu tĩnh trong `inc/data.php`

**Files:**
- Modify: `inc/data.php` (thêm 3 hàm, đặt sau `ecsges_ecosystem_tabs()`)

**Interfaces — Produces:**
- `ecsges_linh_vuc_tabs(): array` — mỗi phần tử `[ 'id'=>string, 'label'=>string, 'image'=>string(tên file trong assets/img), 'paragraph'=>string ]`. 5 phần tử: `huong-nghiep, tuyen-sinh, dao-tao, viec-lam, truyen-thong`.
- `ecsges_ptbv_team(): array` — mỗi phần tử `[ 'name'=>string, 'title'=>string, 'image'=>string ]`. 6 phần tử placeholder.
- `ecsges_ptbv_values(): array` — mỗi phần tử `[ 'title'=>string, 'text'=>string ]`. 3 phần tử.

- [ ] **Step 1: Thêm 3 hàm dữ liệu vào cuối `inc/data.php`** (trước dòng đóng file nếu có, hoặc cuối file)

```php
/**
 * Lĩnh vực hoạt động — 5 tab. Tab HƯỚNG NGHIỆP dùng text thật (Figma);
 * 4 tab còn lại placeholder (thay nội dung sau). 'image' = tên file trong assets/img.
 */
function ecsges_linh_vuc_tabs() {
	$img = 'ecosystem-content.png'; // placeholder ảnh panel — thay ảnh thật sau
	return array(
		array(
			'id'        => 'huong-nghiep',
			'label'     => 'HƯỚNG NGHIỆP',
			'image'     => $img,
			'paragraph' => 'Định hướng tương lai từ sự thấu hiểu năng lực. ECSGES đồng hành cùng học sinh, sinh viên trên hành trình khám phá bản thân, định hình mục tiêu nghề nghiệp và lựa chọn lộ trình học tập phù hợp. Thông qua các chương trình tư vấn, trải nghiệm thực tế và cập nhật xu hướng thị trường lao động, chúng tôi giúp người học xây dựng nền tảng vững chắc để phát triển trong môi trường làm việc hiện đại và hội nhập. Các dịch vụ nổi bật: - Định hướng nghề nghiệp - Lựa chọn ngành học - Phát triển kỹ năng.',
		),
		array( 'id' => 'tuyen-sinh',   'label' => 'TUYỂN SINH',   'image' => $img, 'paragraph' => 'Nội dung đang cập nhật.' ),
		array( 'id' => 'dao-tao',      'label' => 'ĐÀO TẠO',      'image' => $img, 'paragraph' => 'Nội dung đang cập nhật.' ),
		array( 'id' => 'viec-lam',     'label' => 'VIỆC LÀM',     'image' => $img, 'paragraph' => 'Nội dung đang cập nhật.' ),
		array( 'id' => 'truyen-thong', 'label' => 'TRUYỀN THÔNG', 'image' => $img, 'paragraph' => 'Nội dung đang cập nhật.' ),
	);
}

/**
 * Phát triển bền vững — carousel nhân sự (placeholder). 'image' = tên file trong assets/img.
 */
function ecsges_ptbv_team() {
	$img    = 'news.png'; // placeholder chân dung — thay ảnh thật sau
	$people = array();
	for ( $i = 0; $i < 6; $i++ ) {
		$people[] = array(
			'name'  => 'Phan Trần Duy Đạt',
			'title' => 'Giám đốc kinh doanh',
			'image' => $img,
		);
	}
	return $people;
}

/**
 * Phát triển bền vững — 3 giá trị. Chỉ TẬN TÂM có mô tả (theo Figma).
 */
function ecsges_ptbv_values() {
	return array(
		array(
			'title' => 'TẬN TÂM',
			'text'  => 'Chúng tôi tin rằng sự tận tâm là nền tảng của mọi giá trị bền vững. Mỗi cán bộ, giảng viên và chuyên gia của ECSGES luôn làm việc bằng trách nhiệm, sự chân thành và tinh thần phụng sự, hướng đến lợi ích của người học, đối tác và cộng đồng.',
		),
		array( 'title' => 'ĐỒNG HÀNH', 'text' => '' ),
		array( 'title' => 'ĐỔI MỚI',  'text' => '' ),
	);
}
```

- [ ] **Step 2: Lint**

Run: `"D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" -l inc/data.php`
Expected: `No syntax errors detected in inc/data.php`

---

## Task 2: Banner tiêu đề dùng chung `page-hero.php`

**Files:**
- Create: `template-parts/page-hero.php`

**Interfaces:**
- Consumes: `ecsges_img()`, `ecsges_t()` (đã có trong theme).
- Produces: partial gọi bằng `get_template_part( 'template-parts/page', 'hero', array( 'title' => ..., 'id' => ... ) )`. Đọc `$args['title']` (string, bắt buộc), `$args['id']` (string, mặc định `page-hero`), `$args['bg']` (tên file ảnh, mặc định `ve-ecs/ve-ecs-hero-bg.jpg`).

- [ ] **Step 1: Tạo `template-parts/page-hero.php`**

```php
<?php
/**
 * Banner tiêu đề trang (dùng chung cho các trang phụ: Lĩnh vực hoạt động,
 * Phát triển bền vững...). Nhận tiêu đề qua get_template_part($args).
 *
 * @package ECSGES
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

$hero_title = isset( $args['title'] ) ? $args['title'] : '';
$hero_id    = isset( $args['id'] ) ? $args['id'] : 'page-hero';
$hero_bg    = isset( $args['bg'] ) ? $args['bg'] : 've-ecs/ve-ecs-hero-bg.jpg';
?>
<section id="<?php echo esc_attr( $hero_id ); ?>" aria-labelledby="<?php echo esc_attr( $hero_id ); ?>-heading" class="relative isolate flex min-h-[340px] items-center justify-center overflow-hidden lg:min-h-[520px]">
	<img src="<?php echo esc_url( ecsges_img( $hero_bg ) ); ?>" alt="" aria-hidden="true" class="absolute inset-0 -z-10 h-full w-full object-cover object-center">
	<div aria-hidden="true" class="absolute inset-0 -z-10 bg-black/15"></div>
	<div class="px-6 text-center">
		<h1 id="<?php echo esc_attr( $hero_id ); ?>-heading" data-aos="fade-up" class="font-display font-semibold uppercase tracking-[0.06em] text-white text-[34px] sm:text-6xl lg:text-[80px]"><?php echo esc_html( ecsges_t( $hero_title ) ); ?></h1>
	</div>
</section>
```

- [ ] **Step 2: Lint**

Run: `"D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" -l template-parts/page-hero.php`
Expected: `No syntax errors detected`

---

## Task 3: Trang 1 — section tab `linh-vuc-tabs.php`

**Files:**
- Create: `template-parts/linh-vuc-tabs.php`

**Interfaces:**
- Consumes: `ecsges_linh_vuc_tabs()` (Task 1), `ecsges_img()`. Tab switch dựa trên `initEcosystemTabs()` (đã có) qua attribute `data-ecosystem` (root), `data-tab` (button), `data-panel` (panel). JS toggle `bg-brand text-white` ↔ `bg-white text-ink` cho button đang chọn, và `grid` ↔ `hidden` cho panel. (Icon/caret trong JS được null-guard nên không cần.)

**Lưu ý reuse:** `initEcosystemTabs()` dùng `document.querySelector('[data-ecosystem]')` — chỉ 1 match/trang. Trang 1 KHÔNG có section ecosystem (đó là trang chủ), nên `data-ecosystem` ở đây an toàn.

- [ ] **Step 1: Tạo `template-parts/linh-vuc-tabs.php`**

```php
<?php
/**
 * Lĩnh vực hoạt động — thanh 5 tab + panel (ảnh trái, đoạn text phải).
 * Tab tương tác bằng initEcosystemTabs() (assets/js/main.js) qua
 * data-ecosystem / data-tab / data-panel (tái dùng, không cần JS mới).
 *
 * @package ECSGES
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

$lv_tabs = ecsges_linh_vuc_tabs();
?>
<section aria-labelledby="linh-vuc-heading" class="bg-white py-14 lg:py-20">
	<div class="mx-auto w-full max-w-page px-6 md:px-10">
		<h2 id="linh-vuc-heading" class="sr-only">Lĩnh vực hoạt động</h2>

		<div data-aos="fade-up" data-ecosystem>
			<!-- Tab bar -->
			<div role="tablist" aria-label="Lĩnh vực hoạt động" class="flex flex-wrap justify-center gap-x-6 gap-y-3">
				<?php foreach ( $lv_tabs as $i => $tab ) : ?>
					<?php $active = ( 0 === $i ); ?>
					<button
						type="button"
						role="tab"
						id="lv-tab-<?php echo esc_attr( $tab['id'] ); ?>"
						data-tab="<?php echo esc_attr( $tab['id'] ); ?>"
						aria-selected="<?php echo $active ? 'true' : 'false'; ?>"
						aria-controls="lv-panel-<?php echo esc_attr( $tab['id'] ); ?>"
						class="px-6 py-3 text-base font-medium uppercase tracking-wide transition-colors <?php echo $active ? 'bg-brand text-white' : 'bg-white text-ink'; ?>"
					><?php echo esc_html( $tab['label'] ); ?></button>
				<?php endforeach; ?>
			</div>

			<!-- Panels -->
			<?php foreach ( $lv_tabs as $i => $tab ) : ?>
				<?php $active = ( 0 === $i ); ?>
				<div
					role="tabpanel"
					id="lv-panel-<?php echo esc_attr( $tab['id'] ); ?>"
					data-panel="<?php echo esc_attr( $tab['id'] ); ?>"
					aria-labelledby="lv-tab-<?php echo esc_attr( $tab['id'] ); ?>"
					class="mt-10 items-stretch gap-0 md:min-h-96 md:grid-cols-2 <?php echo $active ? 'grid' : 'hidden'; ?>"
				>
					<img src="<?php echo esc_url( ecsges_img( $tab['image'] ) ); ?>" alt="<?php echo esc_attr( $tab['label'] ); ?>" class="h-64 w-full object-cover md:h-full">
					<div class="flex items-center bg-surface-2 p-8 lg:p-12">
						<p class="text-justify text-lg font-light leading-8 text-body"><?php echo esc_html( $tab['paragraph'] ); ?></p>
					</div>
				</div>
			<?php endforeach; ?>
		</div>
	</div>
</section>
```

- [ ] **Step 2: Lint**

Run: `"D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" -l template-parts/linh-vuc-tabs.php`
Expected: `No syntax errors detected`

---

## Task 4: Trang 1 — page template `page-linh-vuc-hoat-dong.php`

**Files:**
- Create: `page-linh-vuc-hoat-dong.php`

**Interfaces:**
- Consumes: `template-parts/page-hero.php` (Task 2), `template-parts/linh-vuc-tabs.php` (Task 3), `template-parts/section-news.php` (có sẵn, `id="tin-tuc"`).

- [ ] **Step 1: Tạo `page-linh-vuc-hoat-dong.php`**

```php
<?php
/**
 * Template Name: Lĩnh vực hoạt động
 * Trang "Lĩnh vực hoạt động". Gán tự động cho Page slug 'linh-vuc-hoat-dong'
 * (nhờ tên file page-linh-vuc-hoat-dong.php) hoặc chọn template.
 *
 * @package ECSGES
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

get_header();
?>
	<main>
		<?php
		get_template_part( 'template-parts/page', 'hero', array( 'title' => 'LĨNH VỰC HOẠT ĐỘNG', 'id' => 'linh-vuc-hero' ) );
		get_template_part( 'template-parts/linh-vuc', 'tabs' );
		get_template_part( 'template-parts/section', 'news' );
		?>
	</main>
<?php
get_footer();
```

- [ ] **Step 2: Lint**

Run: `"D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" -l page-linh-vuc-hoat-dong.php`
Expected: `No syntax errors detected`

- [ ] **Step 3: Build CSS** (các class mới ở Task 2–3 phải được emit)

Run: `npm run build:css`
Expected: build xong, không lỗi; `assets/css/main.css` cập nhật.

---

## Task 5: Trang 2 — carousel JS `initPtbvCarousel()`

**Files:**
- Modify: `assets/js/main.js` (thêm hàm + đăng ký trong `DOMContentLoaded`)

**Interfaces:**
- Produces: `initPtbvCarousel()` — tìm `[data-ptbv-carousel]`; track `[data-ptbv-track]` (flex, các card là con trực tiếp); nút `[data-ptbv-prev]` / `[data-ptbv-next]`. Dịch track bằng `transform: translateX`. perView theo bề rộng: ≥1024→3, ≥640→2, else 1. Clamp index trong `[0, cards.length - perView]`.

- [ ] **Step 1: Đăng ký hàm trong danh sách `DOMContentLoaded`** (sau `initPinsReveal();`)

Sửa khối (khoảng dòng 11–21):

```javascript
  document.addEventListener('DOMContentLoaded', function () {
    initMobileMenu();
    initEcosystemTabs();
    initNewsPagination();
    initStickyHeader();
    initAOS();
    initHeroIntro();
    initCharsReveal();
    initPinsReveal();
    initPtbvCarousel();
    initLangDropdown();
  });
```

- [ ] **Step 2: Thêm hàm `initPtbvCarousel()`** (đặt cạnh `initNewsPagination()`, trước dấu đóng IIFE `})();`)

```javascript
  /* ---------------------------------------------------------------- */
  /**
   * Carousel nhân sự (trang Phát triển bền vững). Dịch track bằng transform.
   * perView theo bề rộng màn hình; clamp trong [0, cards - perView].
   */
  function initPtbvCarousel() {
    var root = document.querySelector('[data-ptbv-carousel]');
    if (!root) return;
    var track = root.querySelector('[data-ptbv-track]');
    var cards = track ? Array.prototype.slice.call(track.children) : [];
    if (!track || cards.length === 0) return;

    var prev = root.querySelector('[data-ptbv-prev]');
    var next = root.querySelector('[data-ptbv-next]');
    var index = 0;

    function perView() {
      var w = window.innerWidth;
      if (w >= 1024) return 3;
      if (w >= 640) return 2;
      return 1;
    }
    function maxIndex() { return Math.max(0, cards.length - perView()); }
    function step() {
      var s = window.getComputedStyle(track);
      var gap = parseFloat(s.columnGap || s.gap || '0') || 0;
      return cards[0].getBoundingClientRect().width + gap;
    }
    function go(i) {
      index = Math.max(0, Math.min(i, maxIndex()));
      track.style.transform = 'translateX(' + (-index * step()) + 'px)';
    }

    if (prev) prev.addEventListener('click', function () { go(index - 1); });
    if (next) next.addEventListener('click', function () { go(index + 1); });
    window.addEventListener('resize', function () { go(index); });
    go(0);
  }
```

- [ ] **Step 3: Kiểm tra cú pháp JS** (Node có sẵn để build CSS)

Run: `node --check assets/js/main.js`
Expected: không in ra lỗi (exit 0).

---

## Task 6: Trang 2 — section carousel `ptbv-team.php`

**Files:**
- Create: `template-parts/ptbv-team.php`

**Interfaces:**
- Consumes: `ecsges_ptbv_team()` (Task 1), `ecsges_img()`. Markup dùng hook JS của Task 5: `data-ptbv-carousel` (root), `data-ptbv-track` (flex track), `data-ptbv-prev` / `data-ptbv-next`.

- [ ] **Step 1: Tạo `template-parts/ptbv-team.php`**

```php
<?php
/**
 * Phát triển bền vững — carousel nhân sự. Điều khiển bằng initPtbvCarousel()
 * (assets/js/main.js) qua data-ptbv-carousel / data-ptbv-track / prev / next.
 *
 * @package ECSGES
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

$ptbv_team = ecsges_ptbv_team();
?>
<section aria-labelledby="ptbv-team-heading" class="bg-white py-14 lg:py-20">
	<div class="mx-auto w-full max-w-page px-6 md:px-10">
		<h2 id="ptbv-team-heading" class="sr-only">Đội ngũ ECSGES</h2>

		<div class="relative px-10" data-aos="fade-up" data-ptbv-carousel>
			<div class="overflow-hidden">
				<div class="flex gap-6 transition-transform duration-500 ease-out" data-ptbv-track>
					<?php foreach ( $ptbv_team as $m ) : ?>
						<article class="relative w-full shrink-0 sm:w-[calc((100%-1.5rem)/2)] lg:w-[calc((100%-3rem)/3)]">
							<img src="<?php echo esc_url( ecsges_img( $m['image'] ) ); ?>" alt="<?php echo esc_attr( $m['name'] ); ?>" class="aspect-[344/393] w-full object-cover">
							<div class="absolute inset-x-0 bottom-0 bg-black/45 p-4 text-white">
								<p class="font-display text-lg font-semibold"><?php echo esc_html( $m['name'] ); ?></p>
								<p class="text-sm font-light"><?php echo esc_html( $m['title'] ); ?></p>
							</div>
						</article>
					<?php endforeach; ?>
				</div>
			</div>

			<button type="button" data-ptbv-prev aria-label="Trước" class="absolute left-0 top-1/2 flex size-8 -translate-y-1/2 items-center justify-center rounded-full bg-white text-ink shadow transition-colors hover:bg-brand hover:text-white">
				<span aria-hidden="true" class="text-xl leading-none">&#8249;</span>
			</button>
			<button type="button" data-ptbv-next aria-label="Sau" class="absolute right-0 top-1/2 flex size-8 -translate-y-1/2 items-center justify-center rounded-full bg-white text-ink shadow transition-colors hover:bg-brand hover:text-white">
				<span aria-hidden="true" class="text-xl leading-none">&#8250;</span>
			</button>
		</div>
	</div>
</section>
```

- [ ] **Step 2: Lint**

Run: `"D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" -l template-parts/ptbv-team.php`
Expected: `No syntax errors detected`

---

## Task 7: Trang 2 — section giá trị `ptbv-values.php`

**Files:**
- Create: `template-parts/ptbv-values.php`

**Interfaces:**
- Consumes: `ecsges_ptbv_values()` (Task 1).

- [ ] **Step 1: Tạo `template-parts/ptbv-values.php`**

```php
<?php
/**
 * Phát triển bền vững — 3 giá trị (TẬN TÂM / ĐỒNG HÀNH / ĐỔI MỚI) + icon tròn.
 *
 * @package ECSGES
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

$ptbv_values = ecsges_ptbv_values();
?>
<section aria-labelledby="ptbv-values-heading" class="bg-surface-2 py-14 lg:py-20">
	<div class="mx-auto w-full max-w-page px-6 md:px-10">
		<h2 id="ptbv-values-heading" class="sr-only">Giá trị</h2>

		<div class="grid gap-10 lg:grid-cols-[auto_1fr] lg:items-start" data-aos="fade-up">
			<div class="mx-auto flex size-40 items-center justify-center rounded-full border-2 border-dashed border-brand/50 lg:mx-0" aria-hidden="true">
				<img src="<?php echo esc_url( ecsges_img( 'earth.svg' ) ); ?>" alt="" class="size-16">
			</div>

			<div class="grid gap-8 sm:grid-cols-3">
				<?php foreach ( $ptbv_values as $v ) : ?>
					<div>
						<h3 class="font-display text-xl font-semibold uppercase text-ink"><?php echo esc_html( $v['title'] ); ?></h3>
						<?php if ( ! empty( $v['text'] ) ) : ?>
							<p class="mt-3 text-justify text-base font-light leading-7 text-body"><?php echo esc_html( $v['text'] ); ?></p>
						<?php endif; ?>
					</div>
				<?php endforeach; ?>
			</div>
		</div>
	</div>
</section>
```

*(Icon tròn: tạm dùng `earth.svg` sẵn có làm placeholder cho icon nét đứt trong Figma — thay icon thật sau.)*

- [ ] **Step 2: Lint**

Run: `"D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" -l template-parts/ptbv-values.php`
Expected: `No syntax errors detected`

---

## Task 8: Trang 2 — page template `page-phat-trien-ben-vung.php`

**Files:**
- Create: `page-phat-trien-ben-vung.php`

**Interfaces:**
- Consumes: `page-hero.php` (Task 2), `ptbv-team.php` (Task 6), `ptbv-values.php` (Task 7). KHÔNG có section news.

- [ ] **Step 1: Tạo `page-phat-trien-ben-vung.php`**

```php
<?php
/**
 * Template Name: Phát triển bền vững
 * Trang "Phát triển bền vững". Gán tự động cho Page slug 'phat-trien-ben-vung'
 * (nhờ tên file page-phat-trien-ben-vung.php) hoặc chọn template.
 *
 * @package ECSGES
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

get_header();
?>
	<main>
		<?php
		get_template_part( 'template-parts/page', 'hero', array( 'title' => 'PHÁT TRIỂN BỀN VỮNG', 'id' => 'ptbv-hero' ) );
		get_template_part( 'template-parts/ptbv', 'team' );
		get_template_part( 'template-parts/ptbv', 'values' );
		?>
	</main>
<?php
get_footer();
```

- [ ] **Step 2: Lint**

Run: `"D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" -l page-phat-trien-ben-vung.php`
Expected: `No syntax errors detected`

- [ ] **Step 3: Build CSS** (class mới ở Task 6–7)

Run: `npm run build:css`
Expected: build xong, không lỗi.

---

## Task 9: Tạo 2 WP Page + gán link nav

**Files:**
- Modify: `inc/data.php` (2 dòng `href` trong `ecsges_nav_items()`, dòng 20–21)

**Interfaces:**
- Consumes: page template Task 4 & Task 8 (WP tự áp theo tên file = slug).

- [ ] **Step 1: Tạo Page 1 qua WP-CLI** (chạy tại `d:\laragon\www\ECS`)

Run:
```powershell
& "D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" "D:\wp-cli\wp-cli.phar" post create --post_type=page --post_status=publish --post_title="Lĩnh vực hoạt động" --post_name="linh-vuc-hoat-dong" --porcelain
```
Expected: in ra ID số của page mới (ghi lại).

- [ ] **Step 2: Tạo Page 2 qua WP-CLI**

Run:
```powershell
& "D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" "D:\wp-cli\wp-cli.phar" post create --post_type=page --post_status=publish --post_title="Phát triển bền vững" --post_name="phat-trien-ben-vung" --porcelain
```
Expected: in ra ID page mới.

- [ ] **Step 3: Xác nhận permalink resolve** (pretty permalinks đã bật)

Run:
```powershell
& "D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" "D:\wp-cli\wp-cli.phar" post list --post_type=page --fields=ID,post_name,post_status
```
Expected: thấy `linh-vuc-hoat-dong` và `phat-trien-ben-vung` trạng thái `publish`.

- [ ] **Step 4: Sửa href nav trong `inc/data.php`** (hàm `ecsges_nav_items()`, dòng 20–21)

Đổi:
```php
		array( 'label' => 'Lĩnh vực hoạt động',   'href' => '#linh-vuc' ),
		array( 'label' => 'Phát triển bền vững',  'href' => '#phat-trien-ben-vung' ),
```
thành:
```php
		array( 'label' => 'Lĩnh vực hoạt động',   'href' => '/linh-vuc-hoat-dong/' ),
		array( 'label' => 'Phát triển bền vững',  'href' => '/phat-trien-ben-vung/' ),
```

- [ ] **Step 5: Lint**

Run: `"D:\laragon\bin\php\php-8.3.28-Win32-vs16-x64\php.exe" -l inc/data.php`
Expected: `No syntax errors detected`

---

## Task 10: Kiểm tra tích hợp end-to-end

**Files:** (không sửa file — chỉ verify)

- [ ] **Step 1: HTTP 200 + đúng nội dung cho 2 trang**

Run:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://ecs.test/linh-vuc-hoat-dong/
curl -s http://ecs.test/linh-vuc-hoat-dong/ | grep -c "LĨNH VỰC HOẠT ĐỘNG"
curl -s -o /dev/null -w "%{http_code}\n" http://ecs.test/phat-trien-ben-vung/
curl -s http://ecs.test/phat-trien-ben-vung/ | grep -c "PHÁT TRIỂN BỀN VỮNG"
```
Expected: `200` cho cả hai; grep trả số ≥ 1.

- [ ] **Step 2: Nav trỏ đúng** (kiểm tra header render link tới 2 slug)

Run:
```bash
curl -s http://ecs.test/ | grep -oE 'href="[^"]*(linh-vuc-hoat-dong|phat-trien-ben-vung)[^"]*"'
```
Expected: thấy `/linh-vuc-hoat-dong/` và `/phat-trien-ben-vung/`.

- [ ] **Step 3: Trang 1 có TIN TỨC, Trang 2 KHÔNG**

Run:
```bash
curl -s http://ecs.test/linh-vuc-hoat-dong/ | grep -c 'id="tin-tuc"'
curl -s http://ecs.test/phat-trien-ben-vung/ | grep -c 'id="tin-tuc"'
```
Expected: trang 1 = `1`; trang 2 = `0`.

- [ ] **Step 4: Kiểm tra bằng mắt trên trình duyệt** `http://ecs.test/linh-vuc-hoat-dong/` và `http://ecs.test/phat-trien-ben-vung/`:
  - Trang 1: banner tiêu đề; click 5 tab đổi panel (HƯỚNG NGHIỆP active cam mặc định); TIN TỨC hiển thị & phân trang chạy.
  - Trang 2: banner; carousel nhân sự — nút ◀ ▶ dịch thẻ; block TẬN TÂM/ĐỒNG HÀNH/ĐỔI MỚI.
  - Nav: click "Lĩnh vực hoạt động" và "Phát triển bền vững" tới đúng trang.
  - Responsive (thu nhỏ cửa sổ): tab bar wrap, carousel về 1 thẻ; không vỡ layout.

---

## Ghi chú tiếp theo (ngoài Phase 1)

- **Assets chính xác từ Figma** (khi hết rate-limit): export banner hero thật, ảnh panel tab, ảnh chân dung nhân sự, icon tròn nét đứt của values → thay các placeholder (`ve-ecs-hero-bg.jpg`, `ecosystem-content.png`, `news.png`, `earth.svg`).
- **Footer link** cho cột "LĨNH VỰC HOẠT ĐỘNG" / "PHÁT TRIỂN BỀN VỮNG" (hiện `href="#"`) — cần refactor dữ liệu footer để mang href; để Phase riêng.
- **Nội dung thật** cho 4 tab còn lại + danh sách nhân sự thật.
- **Phase 2:** migrate toàn theme Tailwind → SCSS (plan riêng).
```
