# Node (chỉ dùng để build SCSS — không đẩy lên)
node_modules/

# Sass cache / source maps
.sass-cache/
*.css.map

# OS / editor
Thumbs.db
.DS_Store

# Tên thiết bị Windows (`> nul` lỡ tay tạo ra file ảo — git luôn thấy nó tồn tại)
nul

# Live Sass output lạc (nếu extension cấu hình sai)
src/scss/main.css

# Công cụ dev cục bộ (không đẩy lên)
.claude/
.playwright-mcp/
.superpowers/
